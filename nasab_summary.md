# Nasab — Feature & Build Summary

_Verified 2026-08-10 against the actual codebase, `supabase/migrations/`, and git history on branch `feature/gate_married_in_family` (HEAD `fd5c63a`, "feat: update version to 0.1.27 and add cunning_document_scanner dependency"); the "Deep linking overhaul" bullets below were added 2026-08-11 as a targeted update after switching to branch `feature/deep_linking`, without a full re-verification pass of the rest of the document. This document is self-contained — it does not assume access to any other file in the repository._

Working tree also has **uncommitted changes** (not yet reflected in any commit): a full "in-married gating" (request & approve) feature — now with two follow-up fix-up migrations on top of the prior baseline's five — plus a new slice of the `relationships` feature; a storage-breakdown bottom sheet (two new RPCs plus new widgets/entities/models); a **Leave Family** flow (a new RPC plus a Family Settings row); a Google Places–backed **location picker** for members/events; and a **deep-link overhaul** replacing plain `app_links` Universal/App Links with the ChottuLink SDK, adding deferred deep linking (an invite code survives a fresh install) plus a public preview RPC for the web fallback page — see "In-progress / uncommitted work" below. The prior baseline's uncommitted items (tree search-a-member/recenter controls, avatar cropping + avatar storage-quota tracking) have since landed in committed history, alongside two newly-committed features: a **Scan Photo** media-source option and (per version bump) app version **0.1.27+44**.

Separately, essentially the entire `lib/` tree shows as modified in `git status` (292 files) — this is **not feature work**: it's a repo-wide `dart format` reflow (collapsing wrapped lines back to one line under the 120-char limit) layered on top of the real diffs above. `CLAUDE.md` itself was just amended, in this same uncommitted diff, with an explicit instruction not to run `dart format` repo-wide going forward (scope it to touched files only) — precisely because of the review noise this run created. Don't read the 292-file count as 292 files of new functionality. (As of the 2026-08-11 deep-linking update, `git status` on `feature/deep_linking` shows a much smaller, ~14-file diff — the repo-wide reflow described here belongs to the prior `feature/gate_married_in_family` snapshot, not the current branch.)

## What Nasab is

Nasab is a Flutter (iOS + Android) family archive app. Users create or join a family, add family members, build a family tree (parents/spouse/children, now in three switchable layouts), get an auto-generated timeline (births, deaths, marriages), post photo/video/audio "memories" with captions and voice notes, organize memories into albums, tag members, comment and like, and manage member profiles. A Free tier is fully usable from signup; a paid **Heritage** tier (via RevenueCat) unlocks higher storage, higher-quality compression, voice notes, video, the radial tree view, and unlimited albums. Backend is Supabase (Postgres + Auth + Row-Level Security) for data/auth, with Firebase Storage for media files, Firebase Remote Config, and PostHog for analytics. Firebase Auth is deliberately not used — the app relies solely on Supabase Auth, and every Storage read/write is gated by a client-side RLS check against Supabase first.

Current app version: **0.1.27+44**.

## Planning docs

Four phase-spec documents live at repo root and are treated as source of truth for their respective scope, in order: `MVP1.md` (original MVP, phases 0–9), `MVP2.md` (growth/polish — push notifications, onboarding, bottom nav, motion, subscriptions groundwork, app icons), `MVP3.md` (Home screen sections — Elder Spotlight, Home Nudges, Upcoming Celebrations, Invite card), `MVP4.md` (Heritage trial & subscription gating — the full paywall/entitlement system described below). `MVP3.md`/`MVP4.md` are not yet referenced from `CLAUDE.md`, which only names the first two.

## Architecture

Feature-first, three layers per feature, no separate "usecase" layer:
- `domain/` — entities and abstract repository interfaces, pure Dart.
- `data/` — models, `XyzRemoteDataSource`, `XyzRepositoryImpl` (the only layers besides `providers/` allowed to touch `supabase_flutter`/`firebase_storage` directly).
- `presentation/` — `screens/`, `widgets/`, `controllers/` (Riverpod codegen `AsyncNotifier`/`Notifier`, named `XyzController`). Screens/widgets only ever call controllers, never datasources or SDKs directly.

State management is Riverpod with code generation (`@riverpod`). `lib/providers/` is the single place dependency-wiring providers live (Supabase/Firebase singletons, per-feature datasource/repository providers, app-wide session/family state) — all `@Riverpod(keepAlive: true)`. `lib/services/` holds cross-cutting wrappers (shared prefs, logging, remote config, storage, analytics, permissions, connectivity, deep links, push notifications, subscriptions, device info, media compression, media job queue), each with a matching keepAlive provider. `lib/styles/` is the single source of truth for colors, typography (Fraunces/Lora/Inter via `google_fonts`), buttons, spacing, radii, and theme — nothing hardcodes a color or `TextStyle` inline. User feedback (errors/confirmations) goes through a custom `AppToast`, never `ScaffoldMessenger`/`SnackBar` directly. Every DB table/column name used in Dart is a constant in `lib/core/constants/db_constants.dart` — no magic strings in datasources. Routing is `go_router` with a `StatefulShellRoute.indexedStack` bottom-nav shell.

**17 feature directories** under `lib/features/`: `auth`, `family`, `feedback`, `home`, `invite`, `member_groups`, `members`, `memories`, `notifications`, `onboarding`, `permissions`, `profile`, `relationships`, `settings`, `subscription`, `timeline`, `tree`.
- Two new features since the last baseline: **`feedback`** (backs the "Delete Account" request flow — insert-only rows triaged by staff, not a general feedback form; "Send Feedback" in Settings is still a plain `mailto:` link) and **`subscription`** (full domain/data/presentation triad for the Heritage paywall/entitlement system — previously just a bare service layer with no UI).
- `features/profile/` still exists as an empty scaffold (domain/data/presentation directories with **zero files inside them**) — actual member-profile UI still lives inside `features/members/presentation/`. Unchanged dead scaffolding.
- `features/permissions/` remains a real, populated feature (runtime-permission priming screen/controller).
- `features/member_groups/` unchanged: personal, per-user shortcut groups of family members used for quickly selecting recipients/tags.

**13 services** in `lib/services/`: `SharedPrefsService`, `LoggingService`, `RemoteConfigService`, `StorageService`, `AnalyticsService`, `PermissionService`, `ConnectivityService`, `DeepLinkService`, `PushNotificationService`, `SubscriptionService`, `DeviceInfoService` (new — feeds `device_details` on feedback rows and other diagnostics), `MediaCompressionService`, `MediaJobStoreService`.

**Provider files** in `lib/providers/` (plus generated `.g.dart` counterparts) now also cover family subscriptions, plan limits, onboarding paywall gating, albums, naming preference, spouse display name, device info, and add-parents onboarding state, alongside the previously-listed auth/family/home/invite/comments/likes/member-groups/members/memories/notifications/permission-priming/profile-media/push-notifications/relationships/storage-quota/media-pipeline providers.

## Feature-by-feature state

- **Auth** — Email/password (two-step bottom-sheet flow) plus Google and Apple social sign-in. A server-side `check_email_exists` function backs a single unified sign-in/sign-up screen.
- **New, uncommitted: Location picker.** A new `lib/core/location/` module (own domain/data/presentation triad — `PlaceSuggestion` entity, `PlaceRepository`, `GooglePlacesRemoteDataSource`, `location_providers.dart`) backs a shared `showPickLocationSheet`/`LocationPickerField` — a debounced, Google Places–backed city/country search sheet (min 3 chars) requiring a new `GOOGLE_PLACES_API_KEY` dart-define. Used for a member's `current_location` (new `family_members` column, shown via `MemberLocationRow` on the profile screen, editable inline) and for `place_of_birth`/`place_of_burial` in Add/Edit Member. A new `events.location` column gives every timeline event a location too: birth/death event location auto-syncs from place of birth/burial (same pattern `event_date` already uses for dob/dod), while marriage events get their own location, settable at creation and editable afterward via a narrow direct `EventRemoteDataSource.updateEventLocation` write. Backed by the `add_member_and_event_locations` migration.
- **Family creation & invite/join** — Create a family or join via invite code; a Postgres RPC (`create_family`) seeds the account holder as a claimed default member. Joining via invite code can claim an existing unclaimed member row instead of always inserting a new one, with a DB-enforced limit on how many families a user can create/join. **Invite codes were single-use by a default-value bug** (`create_family_invite` defaulted `p_max_uses` to 1, never overridden from Dart) — every invite after the first redemption failed with "This invite has already been used." Fixed by raising the default to 200 uses per invite; existing invite rows are unaffected. `list_unclaimed_members` now also surfaces a claimable member's spouse name, to help the right relative pick their own row. A new pair of RPCs, `unclaim_family_member` (owner-only) and `claim_existing_member` (self-serve), let a wrongly-claimed row be detached and re-claimed correctly without deleting the row or losing its relationships/timeline/memories — a lighter-weight fix path than `remove_family_member`'s hard delete.
- **New, uncommitted: Deep linking overhaul (ChottuLink, deferred deep links).** Invite-link handling moved off the previous plain `app_links` Universal/App Links implementation onto the **ChottuLink** SDK (`chottu_link: ^1.1.3`), replacing the earlier hosted domain `nasab.tappstudio.in` with a dedicated `nasab.chottu.link` for the `/f/<code>` invite path everywhere it's referenced (Android `AndroidManifest.xml` intent filter + a new `flutter_deeplinking_enabled=false` meta-data entry to stop Flutter's own auto-handling racing the native SDK, iOS `Runner.entitlements` associated domain, `DeepLinkService`, `InviteShareScreen`). The headline capability gain is **deferred deep linking**: a code tapped in an invite link survives a fresh install (App/Play Store round-trip) and is recovered on first app open, not just on warm/cold start with the app already installed — closing an explicit MVP1/MVP2 gap ("deferred deep linking (e.g. Branch.io-style)" was previously listed as out of scope). `DeepLinkService` drops its old `codeFromUri(Uri)`/`getInitialCode()` split in favor of a single `codeStream` sourced from `ChottuLink.onLinkReceivedWithMeta`, parsed from `ResolvedLink.shortLinkRaw`/`shortLink` (the actual tapped `nasab.chottu.link/f/<code>` URL, not `ResolvedLink.link`, which is a separate per-link browser/store-fallback destination). `InviteShareScreen` now calls `ChottuLink.createDynamicLink` after generating an invite to mint a real `nasab.chottu.link/f/<token>` short link (with social title/description metadata) and prepends it to the shared invite text, falling back silently to code-only sharing if link creation fails; the link's configured fallback destination points at a `nasab-landing` web page (`nasab.tappstudio.in/f/?code=<token>`, a separate repo, not part of this codebase) for a visitor without the app installed. `main.dart` now awaits `ChottuLink.init(apiKey: Env.chottulinkApiKey)` early in boot (unlike the other, deferred-initialized SDKs) since a deferred link's recovery depends on the native SDK being ready before `DeepLinkService`'s listener attaches. New `CHOTTULINK_API_KEY` dart-define and `Env.chottulinkApiKey`. A new **`preview_family_invite_public`** Postgres RPC (anon-callable, narrower than the existing in-app `preview_family_invite`: no `family_id`, just `family_name`/`is_valid`) backs that same `nasab-landing` fallback page showing "Join the &lt;Family&gt; family" to a signed-out visitor before they install.
- **Family members** — First/last name, optional date of birth, country code (added then dropped), `place_of_birth`/`place_of_burial`, contact fields, `is_deceased`, denormalized father-name fields for patronymic display, member deletion, `added_by`. Deceased members are excluded from the "unclaimed members" list. An **avatar thumbnail** column/pipeline (separate from full-res profile media, used across tree nodes, member list, celebrations card, invite claim tiles) backs fast list/card rendering. `family_members.linked_user_id`/`added_by` are `ON DELETE SET NULL` against `auth.users` (previously plain `NO ACTION`, which blocked deleting an auth user linked to or having added a family member). **Avatar cropping** — every avatar entry point (Add/Edit Member, Onboarding) routes through a shared `pickAndCropAvatar` helper (`lib/core/media/avatar_picker.dart`, `image_cropper: ^11.0.0`) that opens a native circular 1:1 crop UI (uCrop on Android, `TOCropViewController` on iOS) with pinch-to-zoom/pan after the gallery pick. Avatars are quota-tracked: they get a real `media` row (`media.is_avatar` boolean) so avatar bytes count toward `family_storage_used_bytes` like every other upload, while staying excluded from the profile-gallery fetch/count and its 5-photo cap; swapping an avatar nets the outgoing file's size out of the quota check so a family at quota can still replace a similarly-sized avatar, and member deletion separately cleans up the avatar's Storage files.
  **New, uncommitted: in-married gating ("request & approve" for gated branches).** A permanent `family_members.married_in` boolean flags any member who entered the tree via a spouse relationship rather than blood descent from the family's founding root (set once at creation by `MemberController.addMember`'s `markAsMarriedIn` param, backfilled by one-time heuristic for pre-existing rows, never recomputed from tree topology afterward). A separate `gated_branch` boolean — true for every `married_in` member and inherited by anyone added underneath them via this flow — propagates the approval requirement indefinitely up the ancestral branch (parent, grandparent, ...), not just the first hop off a married-in member. Adding a **parent or sibling anchored on a gated-branch member** stages the new member as `visibility_status = 'pending'`: the relationship is created immediately via the ordinary `RelationshipsController` cascade (unchanged), but the new row is invisible to everyone except the requester and the family owner until decided. A new `relationship_requests` audit/notification table (RLS: select-only, visible to requester or owner; all writes go through security-definer RPCs, mirroring `family_invites`' pattern) backs three RPCs — `create_relationship_request` (atomically flips the member to pending + inserts the audit row, re-validating the anchor's gated status server-side), `approve_relationship_request` (owner-only status flip; the relationship cascade already ran at request time), and `reject_relationship_request` (owner-only; flips to rejected, then hard-deletes the pending member via the existing `remove_family_member` RPC — rejection deletes rather than keeping the profile around for resubmission). A `can_view_family_member`/`can_view_relationship` pair of security-definer predicates (modeled on `can_view_memory`) re-gate `family_members_select`/`relationships_select`, and every other SECURITY DEFINER function that reads `family_members` directly (`list_unclaimed_members` foremost, since a pending member is `is_claimed = false` by construction and would otherwise leak into invite-claim tiles) was audited and redefined with an explicit `visibility_status = 'active'` filter. Two new Edge Functions (`on-relationship-request-created`, `on-relationship-request-decided`) plus trigger functions notify the owner on submission and the requester on decision, extending `notification_type` with `relationship_request_created`/`approved`/`rejected`. New `relationships` feature slice: `RelationshipRequest`/`RelationshipRequestStatus` entities, `RelationshipRequestsRepository`, `RelationshipRequestsController`, a `PendingRequestsScreen` (owner-only queue, reachable from Settings) with `PendingRequestTile`. Tree nodes and the member profile header render a shared `MemberNodeBadges` caption — "Joined by marriage" for `marriedIn`, or "Awaiting approval" for a pending member (the two never apply to the same member at once) — alongside a new `dashed_circle_painter.dart` for pending-node styling. `visibility_status` is now a typed `FamilyMemberVisibilityStatus` enum (`active`/`pending`) in the `members` domain layer rather than a raw string. **Two follow-up fix-up migrations, also uncommitted:** `get_elder_spotlight` is now gated to skip married-in callers entirely (its only actions walk the caller's own paternal line to suggest adding a father/spouse — exactly the flow this feature routes through Owner approval instead, so a married-in member should never see that nudge); and a bug in the original `married_in` backfill heuristic (zero parent relationships + a spouse relationship) was found and corrected — it also matched each family's **founder** (who likewise has no parent relationships), wrongly flagging founders-with-a-spouse as `married_in`/`gated_branch`. The fix-up migration identifies founders via `family_users.role = 'owner'` (there's no ownership-transfer feature, so this is a reliable 1:1 mapping), resets any founder mismarked by the earlier migrations, and re-runs the backfill excluding founders.
- **Family tree** — Now **three switchable layouts**: the original vertical layout (`family_tree_view.dart`/`tree_layout_engine.dart`), a new **horizontal** layout (`family_tree_horizontal_view.dart`/`tree_layout_transpose.dart`), and a new **radial** layout (`family_tree_radial_view.dart`/`tree_layout_radial.dart`/`radial_tree_layout.dart`) — each with its own canvas, node widget, and connector painter, plus shared dashed "+" placeholder nodes for tree gaps. The radial view is **gated behind Heritage** (`PaywallSource.radialTree`). Patronymic name formatting remains a dedicated shared utility. Spouse relationships still carry current/past status plus a wedding date. **New: search-a-member in tree** — `TreeZoomControls` was reworked from an explicit zoom-in/zoom-out/reset button column with a live zoom-percentage pill down to a simpler two-button **recenter-on-me + search** pair; pinch/drag zoom is now left entirely to the native `InteractiveViewer` (bounded by `minTreeScale`/`maxTreeScale`) instead of button-driven scale steps. Search reuses the existing shared `showMemberSelectBottomSheet` picker and, on selection, pans/recenters the canvas on that member — wired identically into all three layouts' canvases (`tree_canvas.dart`, `tree_canvas_horizontal.dart`, `tree_canvas_radial.dart`). `member_list_view.dart` (the plain member-list screen, separate from the tree) also gained an in-place name search field pinned above the "GEN I" header (`MemberListView` converted from `ConsumerWidget` to `ConsumerStatefulWidget` to hold the query).
- **Timeline** — Auto-generated from birth/death/marriage events only; no manual event creation. Media (including video, now Heritage-gated) can attach directly to a timeline event. Birth events can be undated and are auto-seeded on family creation/join.
- **Memories** — Photo/video/audio posts with a `caption` field, member tags, comments (create/delete, creator-or-owner delete policy), and likes. Memory visibility (`family` vs `restricted`, enforced via the `can_view_memory` security-definer function) is unchanged in mechanism but now also accounts for album membership — a memory filed in an album inherits the **album's** visibility rather than its own for the purposes of `can_view_memory` and the like/comment notification recipient list. **New: Memory Albums** — a full feature (domain/data/presentation triad: `Album`/`AlbumCoverColor` entities, `AlbumRepository`, `AlbumController`, a grid view, a "book"-style slideshow/detail view, a create/edit form sheet, a horizontally-scrollable cover-color picker with a widened palette, a cover-photo picker, an album picker for filing existing memories, delete confirmation, and an empty state — now (uncommitted) its own dedicated `AlbumsEmptyState` widget, mirroring `MemoriesEmptyState`'s layout/artwork/CTA format instead of reusing it directly, plus pull-to-refresh added to the album grid). The plain Memories empty state also picked up new artwork, uncommitted (`AppAssets.polaroidStack`, `assets/png/polaroid_stack.png`). Albums have a per-family cap of **10 on Free, unlimited on Heritage** (`PaywallSource.albumLimit`); a cover photo auto-clears if its underlying media is deleted, and an album's own claimed-cover color/photo persist independently of its contents. Voice notes and richer captions were added to media attachment across memories/profile/timeline media pickers. **New: Scan Photo** — a fourth, flat Add Media option (alongside Open Camera / Upload Photo / Upload Video) backed by `cunning_document_scanner` (VisionKit on iOS, ML Kit's document scanner on Android), for digitizing physical photo prints with auto-crop/straighten; tapping it opens a small nested Scan-a-Photo/Take-a-Photo chooser (the package has no native scan-vs-camera toggle), with "Take a Photo" falling through to the ordinary plain-camera path. A scanned photo becomes an ordinary `MediaType.photo` `PendingAttachment` and flows through the existing Add Memory composer/upload pipeline unchanged; the camera-permission check at all four `showAddMediaSourceBottomSheet` call sites (Add Memory composer + launcher, Timeline event media, Profile media) was widened accordingly since scanning also needs camera access.
- **Member profile** — Lives inside `features/members/presentation/`. Includes profile-attached media (voice notes now Heritage-gated: `PaywallSource.voiceRecorder`) and a spouse-status editing sheet.
- **Home dashboard** — Full feature with real domain/data logic, largely unchanged in shape since the last baseline: Elder Spotlight (paternal-line-aware), Home Nudges (up to 3, distinct types, closeness/date tiebreaks), Upcoming Celebrations (birthdays + anniversaries, local date, avatar thumbnails, 3 at a time), Invite Family card. **New: Home Trial Nudge card** — an owner-only, non-dismissible card reading live subscription state (trial/lapsed) directly from `familySubscriptionControllerProvider` rather than the `get_home_nudges` RPC, since that RPC has no knowledge of subscription state; hidden entirely for Members or while the subscription is `active`.
- **Notifications** — Firebase Cloud Messaging push plus an in-app notification center, user-scoped tables, dismissible (`is_dismissed`). Triggers now cover new member, new memory, tag, **memory like, and memory comment** events (the last two added most recently) plus a daily reminder cron; the like/comment recipient-resolution logic in the Edge Function mirrors `can_view_memory` exactly, including the album-visibility-inheritance rule above. The edge function that sends notifications reads its secret via Supabase Vault.
- **Onboarding & permission priming** — `user_profiles` backs onboarding. The "Add Parents" onboarding step was **split into separate Father/Mother screens** (previously one combined screen) for clearer per-parent data entry, plus a dedicated `AddParentsController`/provider set. A new **patronymic naming preference** (`NamingPreferenceController`, `prefs_keys.dart`) lets a family opt into father-name-based display formatting, feeding the same `PatronymicNameFormatter`/`patronymic_name_text.dart`/`patronymic_name_spans.dart` utilities used across tree, member list, timeline, invite, and home nudge copy. Onboarding now also has a paywall gate step (`OnboardingPaywallGate`) shown once to new Owners, skippable straight into Free.
- **Settings** — Family settings, feedback (`Send Feedback` mailto, plus a proper **Delete Account** flow via a bottom sheet backed by the new `feedback` feature/table), app-store review link, privacy policy/terms links, sign-out, account email, app version. The two long-standing placeholders (privacy/terms URL domain, App Store id `'0000000000'`) are unchanged/still live. Compression quality setting is now plan-gated (Efficient always available; Standard/Premium require Heritage, with narrower carve-outs for family roots — see Subscriptions below).
- **New, uncommitted: Leave Family.** A destructive "Leave Family" row (with a confirm dialog) now sits in Family Settings for non-owners, calling a new `leave_family` RPC. It unlinks the caller's `family_users` row (the thing that actually revokes access) and, if their `family_members` node was claimed, detaches it back to unclaimed — mirroring what `unclaim_family_member` does when an owner does it to someone else, just self-triggered. None of the leaver's contributed content (memories, comments, profile, photos) is deleted; the confirm-dialog copy says so explicitly. Owners are blocked server-side (`cannot_leave_as_owner`, surfaced client-side as a dedicated `CannotLeaveAsOwnerException`) since there's no ownership-transfer path — an owner must delete the family instead. `FamilyController.leaveFamily` follows the `ref.keepingAlive(...)` pattern documented in `CLAUDE.md`'s "Known pitfalls" section (invalidates `myFamiliesProvider`/`currentFamilyProvider`/`hasFamilyMembershipProvider` after the RPC await), and the router's redirect guard carries the user on afterward — to a remaining owned family if one exists, else back to create-or-join.
- **New, uncommitted: Storage breakdown.** Tapping the storage-usage meter in `FamilySwitcherDrawer` now opens `StorageDetailsBottomSheet`, breaking the family's storage usage down three ways: the overall percentage-of-quota (reusing the existing `StorageUsageMeter`), a **by-media-type** breakdown (photos/videos/voice notes), and a **by-uploading-member** breakdown — each rendered by its own section widget (`storage_type_breakdown_section.dart`, `storage_member_breakdown_section.dart`) watching its own provider, so a slow/failed fetch on one doesn't block the others, plus a `storage_upgrade_notice.dart` footer. Backed by two new stable, security-definer RPCs mirroring `family_storage_used_bytes`'s "safe empty result for non-members" shape: `family_storage_by_media_type` (group-by on `media.media_type`/`size_bytes`) and `family_storage_by_member` (left-joins `media.uploaded_by` against `family_members.linked_user_id` to attribute bytes to a claimed member row, grouping uploads from an unclaimed/departed uploader into a single null-`member_id` "Other" bucket so the breakdown always sums to the family's full total). New `StorageMediaTypeBreakdown`/`StorageMemberBreakdown` domain entities and matching data models in the `family` feature.
- **Subscriptions — now a complete, shipped feature** (this was the single biggest gap in the previous baseline; it is resolved). Backed by `MVP4.md`: **Free is a fully usable tier from signup**, not a fallback — no blocking gates anywhere, all gating is contextual at feature entry points. One paid tier, **Heritage**, via RevenueCat (`purchases_flutter`/`purchases_ui_flutter`), family-owned (only the Owner purchases/restores; Members' access follows the family's Postgres row, never a per-device RevenueCat check). The paywall itself is the RevenueCat Paywall Builder rendered via `PaywallView` — no bespoke paywall UI — but Nasab does wrap it with its own screens/widgets: `AppPaywallScreen`, `OnboardingPaywallGate`, `HeritageGateSheet` (the shared entry point for contextual gates, with source-specific copy per `PaywallSource`), `PlanComparisonSheet` (renders live from the `plan_limits` table, not hardcoded copy), and `PaywallMemberFallback` (what a non-owner Member sees instead of a purchase flow). Exactly four/five gated surfaces as of now: voice-note recording (member profiles only), video picking (memories only), storage quota tier, compression quality tier (Efficient/Standard/Premium, with a carve-out allowing Standard for family "roots" and Premium for Heritage), the radial tree view, and the album count cap — each tagged with a `PaywallSource` for analytics. A `RevenueCat` webhook Edge Function (`supabase/functions/revenuecat-webhook`) plus a `subscription_events` audit table (deduped by RevenueCat's own `event.id`) apply `families.plan`/`subscription_status`/`trial_ends_at` transitions server-side; `plan_limits` holds global per-plan defaults (storage quota, allowed compression tiers, max albums) editable without an app release, with per-family override columns available on `families`.
- **Bottom navigation** — Confirmed as exactly **4 tabs**: Home, Tree, Timeline, Memories, via a `StatefulShellRoute.indexedStack`. Unchanged.
- **Motion** — `flutter_animate` + `app_motion.dart` tokens exist; coverage was not re-audited this pass.
- **App icons** — Present and wired via `flutter_launcher_icons`, unchanged.
- **Ops tooling** — New `scripts/` directory: `delete_auth_user.py` (stdlib-only triage/delete tool for `auth.users` rows via the Supabase REST + Admin APIs — diagnostic mode counts every FK-referencing row first, `--force` attempts the actual delete), plus `fetch_family_relationships.py`, `seed_dummy_tree.py`, `seed_test_family.py` for local data seeding/inspection.

## In-progress / uncommitted work

The working tree currently has several feature-complete-but-uncommitted changes (most described inline above, in "Family members" and "Settings"):

- **In-married gating (request & approve)** — seven new migrations, up from five at the last verification: the original five (`add_family_member_married_in`, `add_family_storage_breakdown_rpcs`, `add_relationship_requests`, `add_relationship_request_rpcs`, `add_relationship_request_notifications`, all dated 2026-09-01) plus two newly-added fix-ups dated 2026-09-02 (`gate_elder_spotlight_married_in`, `backfill_married_in_gated_branch` — see "Family members" above for what each does). A new `relationship_requests` table + `married_in`/`gated_branch`/`visibility_status` columns on `family_members`, three new RPCs, two new Edge Functions, and a new slice of the `relationships` feature (`RelationshipRequest` entity/repository/controller, `PendingRequestsScreen`, `PendingRequestTile`) plus shared `MemberNodeBadges`/`dashed_circle_painter.dart` tree-node widgets.
- **Storage breakdown** — `StorageDetailsBottomSheet` and its two section widgets, two new stable RPCs (`family_storage_by_media_type`, `family_storage_by_member`), and matching `StorageMediaTypeBreakdown`/`StorageMemberBreakdown` domain entities/models in the `family` feature.
- **Leave Family** — a `leave_family` RPC plus a new Family Settings row letting non-owner members self-remove without deleting their contributed content; see "Settings" above.
- **Location picker** — Google Places–backed city/country search for a member's current location and place of birth/burial, plus event locations; described in full in the "Auth" bullet above.
- **Deep linking overhaul (ChottuLink)** — swaps `app_links` for the ChottuLink SDK, adds deferred deep linking (invite code survives an install round-trip), a new short-link domain (`nasab.chottu.link`), a real short link minted per invite share, and a new `preview_family_invite_public` anon RPC for the web fallback page; see "Family creation & invite/join" above.

Everything above reads as functionally finished (real RLS/security-definer coverage, notification wiring, UI wired to real providers), not scaffolding — but treat all of it as unverified until it lands in a commit. The prior baseline's uncommitted items (tree search-a-member/recenter controls; avatar cropping + avatar storage-quota tracking) have since been committed and are folded into the sections above without a separate callout.

## Database

**93 migration files** on disk, dated 2026-07-18 through 2026-09-02 — 83 committed plus **10 uncommitted**: the 7 in-married-gating migrations, plus `add_leave_family` (Leave Family), plus `add_member_and_event_locations` (Location picker), plus `add_preview_family_invite_public` (Deep linking overhaul — the anon-callable `preview_family_invite_public` RPC backing the web fallback page). Notable groups beyond the previous baseline:
- Elder spotlight paternal-line rewrite, Home Nudges capped at 3, `is_dismissed` on notifications, local-date celebrations, creator-always-sees-own-memory fix (all carried over from the prior baseline).
- **Subscriptions**: `add_subscription_plan_and_limits` (Phase 1 — `families.plan`/`subscription_status`/`trial_ends_at`, `plan_limits` table), `add_revenuecat_webhook_handler` (Phase 3 — `subscription_events` audit table + apply-webhook-event function), `allow_premium_compression_for_heritage`, `backfill_revenuecat_app_user_id`, `raise_family_invite_max_uses` (unrelated bug fix bundled in the same run), `allow_standard_compression_for_roots`.
- **Members/profile**: `add_family_member_avatar_thumbnail` + `upcoming_celebrations_avatar_thumbnail`, `family_members_user_refs_set_null_on_delete` (FK constraint fix), `list_unclaimed_members_include_spouse_name`, `unclaim_and_reclaim_member`, `add_media_is_avatar`.
- **Memories/albums**: `add_media_caption`, `add_memory_albums`, `widen_album_cover_color_palette`, `add_album_visibility_member_ids_rpc`, `add_album_cover_photo`, `clear_album_cover_on_cover_media_delete`, `clear_claim_on_unlink`.
- **Feedback**: `add_feedback_table` (insert-only, staff-triaged via dashboard).
- **Notifications**: `add_memory_like_comment_notifications`, extending the `notification_type` check constraint and adding `trigger_on_memory_liked`/comment equivalent, with recipient resolution mirroring `can_view_memory` including album-visibility inheritance.
- **In-married gating (uncommitted)**: `add_family_storage_breakdown_rpcs` (two stable RPCs for the storage-details sheet), `add_family_member_married_in` (the `married_in` flag + backfill), `add_relationship_requests` (the `relationship_requests` table, `visibility_status`/`gated_branch` columns, `can_view_family_member`/`can_view_relationship` predicates, RLS policy rewrites, and a full audit of every security-definer function that reads `family_members` directly), `add_relationship_request_rpcs` (`create_relationship_request`/`approve_relationship_request`/`reject_relationship_request`), `add_relationship_request_notifications` (extends `notification_type` with the three `relationship_request_*` values, two new trigger functions handing off to two new Edge Functions), `gate_elder_spotlight_married_in` (excludes married-in members from the Elder Spotlight nudge), `backfill_married_in_gated_branch` (fixes founders wrongly flagged `married_in` by the original backfill's heuristic).
- **Leave Family (uncommitted)**: `add_leave_family` — the `leave_family` RPC (see "Settings" above).
- **Location picker (uncommitted)**: `add_member_and_event_locations` — `family_members.current_location`, `events.location`, and the `add_family_member`/event-creation RPC updates to populate them (see "Auth" above).
- **Deep linking overhaul (uncommitted)**: `add_preview_family_invite_public` — the anon-callable `preview_family_invite_public(p_token)` RPC (family name + validity only, no `family_id`) for the signed-out web fallback page (see "Family creation & invite/join" above).

Row-Level Security is enabled on every table from creation. The core pattern: a user may act on a family-scoped row only if they have a `family_users` row for that `family_id`; only the `owner` role may mutate the `families` row itself or remove other members. `device_tokens`, `user_profiles`, `notifications`, and now `feedback` are user-scoped (`auth.uid()`-only) rather than family-scoped — `feedback` has select/insert-own policies only, no update/delete (users can't edit or retract a submitted request). Security-definer functions (`is_family_member`, `is_family_owner`, `can_view_memory`) are used where a policy needs to read across a table that itself has restrictive RLS.

## CI/CD

A GitHub Actions workflow (`.github/workflows/ios-testflight.yml`) plus Fastlane handles iOS TestFlight deploys (manual code signing, ephemeral CI keychain, Apple Generic Versioning, raised `xcodebuild` timeout, explicit Xcode-version selection, `BUILD_NUMBER` fallback fix, Flutter SwiftPM plugin-copy workaround). Unchanged since the previous baseline.

## Known gaps / near-term housekeeping

- `features/profile/` is still an empty, unused scaffold — either delete it or start using it.
- Two settings placeholders remain live: privacy/terms URLs point at a placeholder domain, and the App Store id is a literal `'0000000000'`.
- One temporary debug route, `/style-guide`, is still wired with an explicit pre-release-removal comment.
- ~~Subscriptions have a complete service/provider layer but no UI entry point~~ — **resolved**: subscriptions now have a full paywall/gating UI shipped (see above). This was the top item in the prior baseline's gap list.
- In-married gating, storage breakdown, Leave Family, the location picker, and the deep-linking overhaul are all complete in the working tree but **not yet committed** (see "In-progress / uncommitted work" above), accounting for all 10 pending migrations already counted in the 93-migration total above.
- The prior `married_in`/`gated_branch` backfill heuristic wrongly flagged family founders as married-in; a fix-up migration (`backfill_married_in_gated_branch`) is staged uncommitted alongside the rest of this feature, so this is fixed pending commit rather than an open gap — but the point-in-time-heuristic root cause described in the next bullet still applies going forward.
- The `married_in`/`gated_branch` backfill for pre-existing members is a one-time point-in-time heuristic, not trigger-maintained — a member who later has all their parent relationships removed (no UI path for that today) won't retroactively flip to `married_in = true`.
- Motion coverage was not re-verified this pass; the prior baseline's "only 12 files use `.animate(...)`" figure should be treated as stale rather than re-confirmed.
- `MVP3.md`/`MVP4.md` exist and are being actively built against but aren't yet cross-referenced from `CLAUDE.md` alongside `MVP1.md`/`MVP2.md`.

## Explicitly out of scope (by design, not yet planned)

OCR/face recognition, advanced automatic relationship detection, PDF/GEDCOM export, offline mode, a web app, an admin dashboard, granular/complex permission tiers beyond owner-vs-member, dark mode, and AI features generally — **deferred deep linking (Branch.io-style), previously listed here as out of scope, has since shipped** (uncommitted; see "Deep linking overhaul" above) — plus (per `MVP4.md`) a Legacy subscription tier beyond Heritage (present only as a disabled "coming later" state on the compression picker, no purchase path) and any custom-built paywall UI beyond wrapping RevenueCat's Paywall Builder — though the tree-completion nudge engine (Home Nudges) and structural relationship-path computation used for the tree remain a plausible foundation for future AI-generated nudge copy or relationship narration, should that be pursued later.
